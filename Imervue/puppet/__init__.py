"""Puppet — built-in 2D rigged-puppet animation workspace.

Lives at ``Imervue.puppet`` and contributes the **Puppet** top-level
tab in :class:`ImervueMainWindow` (slotted right after Paint). The
public surface is :class:`workspace.PuppetWorkspace`; everything else
under this package is implementation detail.
"""
