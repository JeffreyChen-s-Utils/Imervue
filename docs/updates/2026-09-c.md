# docs/updates 2026-09 (c)

Continues [2026-09-b.md](2026-09-b.md), which passed the 800-line batch limit.

## U-20260926-56 · 2026-09-26 · Record U-20260926-54 lost its file names to shell quoting and was repaired in place · #incident #docs

**經過**：記錄 U-20260926-54 時，紀錄內文是放在 Bash 雙引號裡的 `py -3 -c "..."` 字串中產生的。
- 雙引號內的反引號會被 shell 當成命令替換執行。所以內文裡每一段 Markdown 行內程式碼（`tests/test_docs_parity.py`、`puppet_guide.zh-CN.md` 等）都被當成命令執行了：
  - 輸出只有「command not found」；
  - `puppet_guide.<lang>.md` 裡的 `<lang` 被當成輸入重新導向，因找不到檔案而失敗；
  - 沒有產生任何檔案，也沒有其他副作用（事後以 `git status` 與目錄檢查確認）。
- 結果寫進紀錄的是替換後的空字串：U-20260926-54 的內文少了全部的檔名與測試名稱。
- 發現後在 commit `ae474db` 直接把那筆紀錄的檔名補回去。
  - 這違反了本目錄「不改寫已記錄的條目」的規則（批次規則第 5 條），所以補記這一筆，並在 U-20260926-54 末尾加上指向本筆的註記。
  - 補回的只是被吃掉的行內程式碼，其餘文字不變。

**之後的做法**：紀錄內文一律先用檔案寫好（`*_body.md`），再由腳本讀檔組成 spec，不在 shell 字串裡內嵌含反引號的文字。

**批次檔**：`2026-09-b.md` 已經超過 2,000 行，遠超過約 800 行的上限。
- 從這一筆起續寫在 `2026-09-c.md`，並列進 README 的批次表。
- 批次表中 `2026-09.md` 的筆數原本寫 15，實際是 278，一併更正。

## U-20260926-57 · 2026-09-26 · 65-point and 1D LUTs load, Graduated Density offers its tint, and the library docs match the code · #fix #lut #develop #docs #i18n #Imervue_Plugins

**問題**：用唯讀代理人逐節核對 docs 的「圖庫與中繼資料管理」「進階顯影與合成」兩章與程式，找到兩個功能問題與十處文件錯誤。

功能問題：
- **`.cube` LUT 的尺寸上限寫錯**：`lut.py` 只有一個 `_MAX_CUBE_SIZE = 64`，1D 和 3D 共用。
  - Adobe Cube LUT 規格 1.0：1D 表 N ∈ [2, 65536]（6.1 節），3D 格 N ∈ [2, 256]（7.1 節）。
  - 所以常見的 1024／4096 點 1D 曲線全部被拒；DaVinci Resolve 與 ARRI 匯出的 **65³** 3D LUT 也會被拒，這是最常見的尺寸之一。
- **漸層減光鏡（Graduated Density）沒有色調可選**：
  - `apply_graduated_density` 本來就接受 `tint`（每個色版的乘數），README 也寫「with an optional tint」、docs 寫「optionally tinted」；
  - 但對話框只有角度、密度、硬度、偏移四個滑桿，從不傳 `tint`。

文件錯誤（英文，9 個語系跟著翻譯）：
- 相似圖片搜尋的欄位叫「Max Hamming distance」，不是「Max distance」。
- 自動標籤的清單漏了常見的 `auto/graphic`（不是文件、截圖或相片時就歸到這類）。
- 「Index Keywords」在右鍵 > Batch Operations 底下，而且要先選取縮圖。
- XMP 顏色標籤：只有新的或改過的顏色才照 Lightroom 的寫法匯出；sidecar 原本就是 Bridge 的同色用詞時，會保留原用詞（`xmp_sidecar.py`）。
- 時間軸與月曆的日期來源少了一層：實際依序是 DateTimeOriginal → DateTimeDigitized → **DateTime** → 檔案修改時間。
- 鏡頭校正「不放進 recipe 是因為輸出尺寸可能改變」：輸出尺寸永遠和原圖相同，這個理由不成立。
- 地圖檢視：
  - 取用的是目前開啟的資料夾，不是整個圖庫；
  - 每個最近城市一個標記並顯示張數，不是每張圖一個標記；
  - 沒有 WebEngine 時，後備清單列的也是這些地點。
- 裁切／拉直的角度不是「任意」，滑桿範圍是 ±15°。
- 「色調與創意效果」說全都是純 NumPy：「邊框與說明文字」是用 Pillow 繪製的。
- 網頁相簿：只有勾「複製原始檔案」時才能不靠伺服器、放到任何靜態主機；不勾時，大圖連結指向本機硬碟上的檔案。

核對過、確認正確的小節（不改）：Library Search、Smart Albums、Semantic Search、Token Batch Rename、Metadata Export、挑片、暫存匣、雙窗格檔案管理、Tone Curve、Virtual Copies、HDR、全景、景深合成、修復筆刷、人臉偵測、局部調整遮罩、分離色調、仿製印章、自動拉直、降噪銳化、天空替換、軟打樣、GPS 地理標記、列印排版。

**修正**：
- `lut.py`：拆成 `_MAX_1D_SIZE = 65536`（規格上限）與 `_MAX_3D_SIZE = 65`，錯誤訊息標明是 1D 或 3D。
  - 3D 停在 65 是實測的結果：65 點（274,625 列）解析約 0.7 秒，129 點約 11 秒；規格允許到 256，但那種尺寸純 Python 解析會拖太久。
  - 對話框提示、模組說明、5 種內建語言與西班牙文外掛的 `lut_hint`，以及 README／docs 10 個語系，都從「最高 64³」改成「3D 最高 65³、1D 最多 65,536 點」。
- 漸層減光鏡對話框加一個「為漸層那一側加上色調」勾選框與色塊（沿用 U-20260926-42 的 `ColorSwatchButton`）。
  - 預設是 (204, 230, 255)，也就是濾鏡說明裡「讓變暗的一側偏冷」的 (0.8, 0.9, 1.0)；
  - 勾選後色塊才可用；套用時由 `tint_multiplier` 換成 0–1 的乘數傳給濾鏡。
  - 新增 1 個翻譯鍵（5 種內建語言）。
- 上述十處文件錯誤在 docs 10 個語系全部更正（LUT 上限另外也在 README 10 個語系更正）。
- 西班牙文外掛同步到 Imervue_Plugins `main`。

**驗證**：
- `tests/test_lut.py`（新增 3 項，改寫 1 項）：
  - 65 點 3D identity LUT 可以載入，表格形狀是 (65, 65, 65, 3)；
  - 4096 點 1D 曲線可以載入；
  - 66 點 3D 與 65,537 點 1D 各自被拒，錯誤訊息分別指出 3D／1D 與上限。
- `tests/test_graduated_density_dialog.py`（新增 4 項）：
  - 色調預設關閉，色塊跟著勾選框啟用；預設色是 (204, 230, 255)；
  - 關閉時 `tint_multiplier` 回傳 None；(255, 0, 51) 換成 (1.0, 0.0, 0.2)；
  - 勾選並選 (255, 128, 0) 後套用，濾鏡收到的最後一個參數是 (1.0, 128/255, 0.0)。
- `test_docs_parity` 通過，各語系 docutils 比對訊息不變，`rec.py rst` 與基準相同，完整測試通過。

## U-20260926-58 · 2026-09-26 · Comments read as sentences again after the brand-name scrub · #refactor #comments

**問題**：c0d92fa（擁有者決定拿掉第三方品牌比較用語）以機械式替換把品牌名改成通稱，結果在程式註解與 docstring 留下不成句的文字。
- 「raster paint apps's」（複數加 's）共 42 處，集中在 `Imervue/paint/`。
- 「other XMP-aware photo managers's Workspaces」「think other XMP-aware photo managers workspaces」。
- 「This is the classic other XMP-aware photo managers vibrance trick」。
- 「Adobe, other XMP-aware photo managers, and most free LUT packs」。
- 動作面板的「raster paint apps's brush-preset list / other XMP-aware photo managers's preset preview」。
- 找到經過：翻譯代理人回報 zh-tw 文件有「date-grouped library views 樣式」這種殘字，追查到 c0d92fa，掃描全 repo 同類字串。使用者看得到的介面字串與文件另開一筆處理。

**修正**：只改文法，不恢復任何品牌名，也不改任何程式行為。
- 42 處「raster paint apps's」改成複數所有格「raster paint apps'」。
- `workspace_dialog.py`、`workspace_manager.py`、`lut.py`、`recipe_adjustments.py`、`puppet/motion_dock.py` 的上述句子改寫成通順的中性說法（例如「the classic vibrance trick」「Adobe tools and most free LUT packs」「photo-manager workspaces」）。
- `Imervue/image/xmp_sidecar.py` 的兩處本來就通順（「XMP-aware photo managers write one automatically」「readable by other XMP-aware photo managers」），而且另一個工作階段正有未提交的修改，所以不碰。

**驗證**：ruff 通過；完整測試通過；再掃描 `apps's`／`managers's`，已經一處都不剩。

## U-20260926-59 · 2026-09-26 · The hover preview and the hovered-tile keys keep working after a click on the wall · #fix #browse #hover #culling

**問題**：在縮圖牆上點過一次之後，懸停預覽（游標停在縮圖上 500 ms 跳出大圖）就再也不出現。評分、顏色標籤、挑片鍵的「游標下的縮圖」目標也跟著失效。
- 左鍵按下時，`view_mouse.mousePressEvent` 會記下 `_drag_start_pos`，等拖動超過門檻才開始框選。
- 放開時走 `InputController.handle_tile_release`：
  - 只有真的框選過（`finish_drag_select`）或拖出檔案時才會把它清掉；
  - 一般的單擊（開啟圖片、選取模式下切換選取、點在空白處）都沒清。
- 之後每次移動滑鼠，`update_hover_preview` 看到 `_drag_start_pos` 還在，就當成「正在拖動」而取消預覽，並清掉 `_hover_last_path`。
  - 所以預覽再也不會出現；
  - `resolve_cull_targets` 依游標找縮圖的那一層也永遠落空：沒選取、焦點框也沒顯示時，按 1–5、F1–F5、P／Shift+X／U 都沒有對象。
- 找到經過：唯讀代理人核對 docs「瀏覽圖片」一章時，追到這個狀態沒被重設；這裡以測試重現。

**修正**：`handle_tile_release` 只要不是框選收尾（也就是一次點擊），就先清掉 `_drag_start_pos` 與 `_drag_end_pos`，再處理開啟或切換選取。

**驗證**：`tests/test_input_controller.py`（新增 4 項，修正前全部失敗）
- 一般模式與選取模式下點一張縮圖後，`_drag_start_pos`／`_drag_end_pos` 都是 None。
- 點在縮圖牆空白處（回傳 False）後，也是 None。
- 選取模式下點一下（留在縮圖牆上），再把游標移到另一張縮圖：懸停預覽會啟動，`_hover_last_path` 是那張圖。
- 完整測試通過。

## U-20260926-60 · 2026-09-26 · Sentences the brand-name scrub broke read properly again in the app and the docs · #fix #i18n #docs #xmp

**問題**：c0d92fa（擁有者決定拿掉第三方品牌比較用語）用機械式替換把品牌名換成通稱，在使用者看得到的地方留下不成句、甚至重複或沒翻譯的文字。

介面字串（5 種內建語言）：
- XMP 伴隨檔對話框的說明是「Read or write XMP-aware photo managers-compatible .xmp sidecar files … round-trip with XMP-aware photo managers, Bridge, XMP-aware photo managers.」：原本的兩個品牌被換成同一個詞，於是重複。
- 中、日、韓三種語言的同一句裡，直接夾著沒翻譯的英文「XMP-aware photo managers」。
- 挑片鍵 `P` 的提示：英文是「a Pick — flag-based keeper flag」；中日韓版本也夾著「XMP-aware photo managers 風格」。

README 與 docs（10 個語系）：
- 「Culling — other XMP-aware photo managers 3-state flag」「XMP Sidecar (other XMP-aware photo managers Interop)」這種標題。
- 「round-trip cleanly with other XMP-aware photo managers, other XMP-aware photo managers, Bridge」這種重複清單。
- 「the same way other XMP-aware photo managers switches *Library* / *Develop*…」。
- 「It does what Live2D do」（原本是「Live2D / Inochi2D」，只刪掉一半）。
- 「Split Toning — flag-based shadow / highlight hue」「Independent flag-based colour flags」「flag-based three-state cull flag」。
- 「Register programs (your image editor /  / …)」：兩個品牌被刪掉，留下空格。
- 總覽表的交互參照「See *Paint Tab — full-featured Raster Editor*」指向不存在的標題。
- 翻譯版中：ja、ko、zh-cn 的時間軸那句還寫著「Google Photos 風格」，是擁有者已經拿掉的品牌；zh-tw 則是「（date-grouped library views 樣式）」這種殘字；zh 的總覽表甚至混進日文「本格的なラスター描画」。
- 同時補上兩處 README 殘留：「arbitrary-angle straighten」（實際最多 ±15°）與「right-click **Index Keywords**」（實際在 Batch Operations 底下）。
- 找到經過：翻譯代理人在 zh-tw 看到「date-grouped library views 樣式」回報，追查到 c0d92fa 後掃描整個 repo。

**修正**：不恢復任何品牌名，只把句子寫通。
- XMP 說明改成「讀寫每張圖旁的 .xmp 伴隨檔……可與 Adobe Bridge 及其他支援 XMP 的相片管理軟體互通」。Adobe Bridge 本來就沒在拿掉的清單裡，文件其他地方也以它的顏色標籤用詞為準。
- `P` 的提示改成「標為 Pick — 要保留的照片」。
- README／docs 的上述句子改寫成中性說法：
  - 「three-state flag」；
  - 工作區一段拿掉比較句；
  - Puppet 簡介直接列出功能；
  - XMP 一節的標題改成對話框本身的「XMP Sidecars」；
  - 交互參照改成實際的「Paint Workspace (Paint Tab)」。
- 9 種翻譯的 README 與 docs 一併更正，中日韓採用介面的實際標籤，並清掉其中夾雜的英文片語與品牌名。
- 程式註解的同類問題另見 U-20260926-58。

**驗證**：
- `test_docs_parity` 通過；各語系 docutils 比對訊息不變，改過的標題底線長度也足夠；`rec.py rst` 與基準相同。
- 再掃描 18 個翻譯檔：非英文句子裡已沒有「XMP-aware photo managers」「date-grouped library views」「raster paint apps」或被拿掉的品牌名。
- 介面字串只有 XMP 說明與 `P` 的提示改變；翻譯鍵檢查與完整測試通過。

## U-20260926-61 · 2026-09-26 · Paint's options bar, all-layer fill and layout saving work, and closing asks about unsaved Paint tabs · #fix #paint #autosave #data-loss #docs #i18n #Imervue_Plugins

**問題**：用唯讀代理人逐節核對 README 與 docs 的 Paint 一章，找到一批「看得到卻沒有作用」的控制項、一個會丟失資料的關閉流程，以及二十多處文件錯誤。

控制項與行為：
- **上方選項列的幾頁全是擺設**：
  - `tool_bar.py` 的填色、選取、文字、漸層四頁都是 `@staticmethod` 建出來的，拿不到工具狀態，任何控制項都沒接上。
  - 選取的 Replace／Add／Subtract／Intersect 永遠是 Replace；漸層的 Linear／Radial／Angle／Diamond 與 Reverse 永遠是 Linear。
  - 填色的 Tolerance、Contiguous、Sample all layers 不起作用，而且 Contiguous 一開始顯示「未勾」，實際上卻是連續填色。
  - Feather 滑桿背後根本沒有羽化功能。
  - 快速選取沒有對應的頁，切過去時選項列停在上一個工具的頁。
  - 文字頁的字型、大小、粗體、斜體和點畫布時跳出的 Add Text 對話框重複，而且也沒有作用。
- **「取樣所有圖層」從來沒有生效**：填色工具只看 `use_reference_layer`，從不讀 `sample_all_layers`，筆刷面板的同名勾選框也一樣沒用。
- **Workspace Layouts 的「Save current…」什麼都不存**：對話框送出 `save_requested`，但 `settings_menu` 只接了 `apply_requested`。
- **關閉 Imervue 不會問 Paint 有沒有未儲存的分頁**：
  - `PaintWorkspace.closeEvent` 裡的確認只會在它自己是視窗時觸發；它是主視窗的一個分頁，永遠收不到 closeEvent。
  - 主視窗關閉最後又走 `os._exit(0)`，所以未儲存的繪圖直接消失。
- **自動儲存從沒啟動**：
  - `start_autosave` 沒有任何呼叫者，所以不會有快照，文件寫的狀態列提示與啟動時的還原提示都不會出現；
  - 還原提示要使用者去的「File ▸ Restore」選單也不存在。
- 找到經過：Paint 一章的文件稽核；其中選項列與關閉流程是追查「文件寫的功能為什麼按了沒反應」時確認的。

**修正**：
- `PaintOptionsBar` 的填色／選取／漸層頁改成實例方法：
  - 控制項從工具狀態取初值，變更時寫回 `ToolState`（`set_fill`、`set_selection_mode`、`set_gradient`）；
  - 別處改了狀態也會反映回來。
- 快速選取也用選取頁。
- 拿掉沒有功能的 Feather 滑桿與重複的文字頁；文字工具改用共用的「此工具沒有選項」頁，選項在 Add Text 對話框裡。
- `FillTool` 在「取樣所有圖層」開啟時以畫面合成影像比對顏色；參考圖層同時開啟時仍以參考圖層為準。
- `capture_workspace_preset` 記下六個面板目前是否顯示（用 `isHidden`，所以分頁排在後面的面板也算顯示中），「Save current…」存成使用者版面；用到內建名稱時跳出說明。
- `PaintWorkspace.confirm_close()`：
  - 有未儲存分頁就詢問，取消時回傳 False；
  - 同意時停止自動儲存、刪掉自己寫的快照，並存面板配置。
  - 主視窗 `closeEvent` 一開始就先問它，取消則整個關閉取消。
- 自動儲存：
  - 建構時啟動，每 2 分鐘一次；只有作用中分頁有未儲存修改時才寫快照，所以沒動過的畫布不會在下次啟動時誤報可還原。
  - 檔案選單新增「Restore Autosave」，把最新快照載入作用中分頁並提示結果；還原提示改指向這個項目。
- 新增 5 個翻譯鍵（5 種內建語言與西班牙文外掛）。
- README 與 docs 的 Paint 一章在 10 個語系全部改成程式實際的行為。例如：
  - 筆刷種類是 Pencil／Pen／Marker／Airbrush／Watercolour／Sumi；
  - 仿製印章用 Alt+按一下取來源；
  - Sponge 只會去飽和；
  - 對話框濾鏡沒有即時預覽；
  - 開啟 PSD 會攤平成一個圖層；
  - 匯出影像是 PNG；
  - 動畫影格沒有匯出，洋蔥皮只顯示前一格；
  - 剪裁遮罩是把作用中圖層裁到下方圖層；
  - 參考圖層是填色的比對來源。
- 西班牙文外掛同步到 Imervue_Plugins `main`。

**驗證**：
- `tests/test_paint_tool_bar.py`：填色頁從狀態取初值並寫回；選取模式與漸層種類／反轉寫回狀態；別處改狀態時選項列跟著變；文字工具落在共用的空白頁；每個工具都有頁，快速選取落在選取頁。
- `tests/test_paint_fill.py`：
  - 「取樣所有圖層」開啟時，空白圖層上按在合成影像的黑方塊內，只填滿那個方塊；
  - 關閉時整張空白圖層一起被填；
  - 參考圖層優先。
- `tests/test_paint_workspace_preset_dialog.py`：記下面板顯示狀態；存成使用者版面；內建名稱被拒並提示。
- `tests/test_paint_workspace_autosave.py`（新檔，不需要 GL，CI 也會跑）：
  - 只有未儲存時才寫快照；
  - 只刪自己寫的快照；
  - `confirm_close` 三種情況（沒有未儲存、同意捨棄、取消）；
  - 「Restore Autosave」成功與沒有快照時的提示。
- `tests/test_main_window_close.py`：Paint 回答取消時只記錄 `event.ignore`、不做任何收尾；同意時照原本順序收尾並結束。
- 這次稽核另外找到的問題記進 `progress.md` #32–#35。
- 修正前以上新測試全部失敗；`test_docs_parity`、docutils 比對與完整測試都通過。

## U-20260926-62 · 2026-09-26 · New Paint tabs get Hand, Zoom, the bracket keys and the right-click menu, and the Pressure Curve shapes pen pressure · #fix #done #paint #tablet #docs #i18n

**問題**：
- **第二個以後的 Paint 分頁少了兩樣接線**。`workspace_tabs.new_tab` 建出的畫布只接了工具派發器：
  - 沒有 `set_size_hud`，拿不到工具狀態，所以 Hand、Zoom 工具，還有 `[` / `]` 調筆刷大小，都只在第一個分頁有作用；
  - 沒接 `customContextMenuRequested`。畫布的右鍵選單設成自訂，沒接上就什麼都不跳。
  - 用「開啟」或「新增分頁」開出來的分頁都有這個問題。
- **Settings > Pressure Curve 存下的曲線沒有任何筆畫會讀**。對話框把曲線掛在工具狀態上當臨時屬性，沒有存檔；筆刷與橡皮擦直接拿原始筆壓。
- **用過繪圖筆之後，滑鼠畫的線會變細變淡**。畫布把筆的最後壓力存在 `_last_pressure`，滑鼠事件也拿它來用；抬筆那一刻的壓力是 0，於是之後每一筆滑鼠都只剩 30% 大小、10% 不透明度，直到再用筆為止。
- 找到經過：追查 Paint 文件稽核時發現「Pressure Curve 沒有作用」，順著畫布的輸入路徑查到另外兩個問題。

**修正**：
- `new_tab` 比照第一個分頁，接上工具狀態與右鍵選單。
- `ToolState.pressure_curve` 成為正式欄位：
  - 預設是恆等曲線；
  - 有 `set_pressure_curve`，會存檔並送出 `EVENT_PRESSURE_CURVE`；
  - 存進 `paint_state`；缺少或格式錯誤時回到恆等曲線。
  - 設定選單的 OK 改走 `set_pressure_curve`。
- 畫布的繪圖板事件把原始筆壓套上曲線（`_pen_pressure`）後才交給工具；滑鼠事件一律用 1.0。
- 刪掉只剩寫入、沒有讀取的 `_last_pressure`。
- 10 個語系的 README 與 docs 補上曲線的用法（拖曳控制點、點一下新增、右鍵刪除、Linear / Soft / Hard 起始曲線），並寫明滑鼠以全壓力作畫。

**驗證**：
- `tests/test_paint_workspace_tabs.py`：新分頁拿到工具狀態與大小提示，右鍵訊號會呼叫選單。
  - 拿掉修正時這支測試失敗。
- `tests/test_paint_canvas_input.py`（新檔，不需要 GL，CI 也會跑）：
  - 筆壓照曲線換算：Hard Floor 在 0 得 0.4、在 0.1 得 0.45；
  - 用過筆（`_last_pressure` 為 0）之後，滑鼠一筆仍以 1.0 送出。
- `tests/test_paint_tool_state.py`：
  - 預設是恆等曲線；
  - 設定時存檔並只送出一次事件，存檔內容可以讀回相同曲線；
  - 缺少或格式錯誤的存檔值回到恆等曲線。
- `tests/test_paint_settings_menu.py`：對話框編輯後按 OK，曲線寫進狀態與設定檔；取消則維持恆等曲線。
- `test_docs_parity`、RST 檢查與完整測試都通過。

## U-20260926-63 · 2026-09-26 · Hide on fullscreen works from the first launch and brings the pet back · #fix #desktop-pet

**問題**：Desktop Pet 的「全螢幕時隱藏」有兩個問題：

- **預設是開，但啟動後根本沒在偵測**。偵測器只在 `set_hide_on_fullscreen` 裡建立，`PetWindow` 建構時是 `None`。分頁上的勾選框在接上訊號前就先勾好，所以沒有任何呼叫會去建它。只有使用者先取消再勾一次才會生效。
- **被隱藏之後就不會回來**：
  - 自動隱藏走 `self.hide()`；
  - `hideEvent` 會把偵測器停掉；
  - 輪詢一停，就再也看不到「全螢幕結束」。
- 找到經過：Puppet／Desktop Pet 文件稽核核對這個選項的說明時發現。

**修正**：
- `PetWindowFlagsMixin._ensure_fullscreen_detector()` 第一次用到時才建立偵測器。
- 視窗的 `showEvent` / `hideEvent` 改呼叫兩個掛勾：
  - `_watch_fullscreen_on_show`：設定開著就開始輪詢。使用者在全螢幕期間手動叫出寵物，視同蓋過自動隱藏，不再算是「被全螢幕藏起來」。
  - `_watch_fullscreen_on_hide`：除非是全螢幕把它藏起來，否則停止輪詢。全螢幕藏起來時繼續輪詢，全螢幕結束時才能把寵物叫回來。

**驗證**：`tests/test_desktop_pet_window_flags.py`（新檔）用 QObject 替身，它的 show / hide 走跟真視窗一樣的掛勾，所以不需要 GL，CI 也會跑。
- 設定開著時，第一次顯示就開始輪詢；關著時不建立偵測器。
- 全螢幕開始時寵物隱藏且仍在輪詢；結束時寵物回來。
- 使用者自己隱藏時停止輪詢。
- 全螢幕期間手動叫出、再手動隱藏，全螢幕結束時寵物仍保持隱藏。
- 已顯示時打開設定會開始輪詢並存檔，關掉則停止。
- 把掛勾換回舊行為時，其中 3 支測試失敗。
- 完整測試通過。

## U-20260926-64 · 2026-09-26 · The Cubism import notice names the environment variable the importer reads · #fix #puppet #i18n

**問題**：匯入 `.moc3` / `.model3.json` 時跳出的 Cubism 說明，以及「找不到 SDK」的提示，都叫使用者設定 `LIVE2D_CUBISM_CORE` 環境變數。但 `cubism_native_bridge` 讀的是 `CUBISM_CORE_DLL`（`LIBRARY_ENV_VAR`），照提示做的人永遠找不到 DLL。

- 受影響的是 5 種內建語言，以及 `workspace_import.py` 的英文備用字串；西班牙文外掛沒有這兩個鍵，會用英文。
- 使用手冊寫的本來就是正確的 `CUBISM_CORE_DLL`。
- 找到經過：Puppet／Desktop Pet 文件稽核對照手冊與程式時發現。

**修正**：兩段字串在每種語言與備用字串都改成 `CUBISM_CORE_DLL`。

**驗證**：`tests/test_translations.py` 新增參數化測試，5 種語言的兩個鍵都要提到 `LIBRARY_ENV_VAR`，而且不能再出現舊名稱。修正前這 10 個案例都失敗。完整測試通過。

## U-20260926-65 · 2026-09-26 · Saved workspaces keep the split between the folder tree and the viewer · #fix #workspace #layout

**問題**：儲存的工作區從來沒記下檔案樹與檢視器之間的分隔位置。

- `workspace_dialog.capture_current_workspace` / `apply_workspace` 讀的是 `ui._main_splitter`；
- 主視窗從沒設定這個屬性，那個分隔器只是建構時的區域變數；
- 所以 `splitter_sizes` 永遠是空的，套用時也跳過。
- 手冊寫工作區會存「分隔器大小」，舉的例子正是「收起檔案樹」的配置。
- 找到經過：其餘章節的文件稽核。

**修正**：主視窗把檔案樹 | 檢視器的分隔器存成 `self._main_splitter`。

**驗證**：
- `tests/test_main_window_layout.py` 的屬性清單加上 `_main_splitter`。
- 新測試先把分隔設成 250 / 750，存成工作區後，要記下兩個大小，且等於分隔器目前的大小。修正前這支測試因為沒有這個屬性而失敗。
- 完整測試通過。

## U-20260926-66 · 2026-09-26 · Create GIF / Video makes MP4s with the bundled ffmpeg and from odd-sized pictures · #fix #done #video #export

**問題**：「建立 GIF／影片」的 MP4 有兩個問題：
- **只找 PATH 上的 ffmpeg。**
  - `imageio-ffmpeg` 是預設相依套件，本身附帶 ffmpeg，但這個對話框沒有用到。
  - 所以沒自己裝 ffmpeg 的使用者，每次輸出 MP4 都失敗，錯誤是「ffmpeg not found in PATH」。
  - 這台開發機就是這種狀況。
- **影格寬或高是奇數時整支影片失敗。** libx264 搭配 yuv420p 不接受奇數尺寸。
  - 實測 63 × 47 的影格：ffmpeg 回報 `width not divisible by 2`，沒有輸出檔。
  - 尺寸不一的影格則由 ffmpeg 自動縮到第一張的大小，沒有問題。
- 找到經過：其餘章節的文件稽核，加上用 `imageio-ffmpeg` 內附的 ffmpeg 實測。

**修正**：
- 新增 `find_ffmpeg()`：先找 PATH，找不到再用 `imageio_ffmpeg.get_ffmpeg_exe()`；兩邊都沒有才回報錯誤，並說明可以安裝 `imageio-ffmpeg` 或把 ffmpeg 放上 PATH。
- ffmpeg 指令加上 `pad=ceil(iw/2)*2:ceil(ih/2)*2`，奇數邊補一個像素。

**驗證**：
- `tests/test_gif_video_dialog.py`：
  - PATH 上沒有時，用 `imageio-ffmpeg` 提供的路徑；
  - PATH 上有時優先用 PATH；
  - 兩邊都沒有（沒有執行檔，或套件不存在）時回傳 None；
  - 用 3 張 63 × 47 的影格，真的做出非空的 MP4。
- `tests/test_gif_video_cancel.py` 的「沒有 ffmpeg 只回報一次失敗」回歸測試，前提改成同時藏起 `imageio-ffmpeg`，讓它仍然走到缺少 ffmpeg 的路徑。
- 完整測試通過。
