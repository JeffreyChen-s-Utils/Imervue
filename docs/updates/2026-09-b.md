# docs/updates 2026-09 (b)

Continues [2026-09.md](2026-09.md), which passed the 800-line batch limit.

## U-20260925-62 · 2026-09-25 · AVIF opens and saves without pillow-heif; the install hint is for HEIC only · #fix #formats #avif

**問題**：AVIF 一直被當成 pillow-heif 的格式，但 pillow-heif 從 1.0 起就拿掉了 AVIF（[pillow_heif CHANGELOG](https://github.com/bigcat88/pillow_heif/blob/master/CHANGELOG.md)），改由 Pillow 自己讀寫：Pillow 11.3 起的 wheel 內含 libavif，用 dav1d 解碼、aom 編碼（[Pillow 11.3.0 release notes](https://pillow.readthedocs.io/en/stable/releasenotes/11.3.0.html)）。專案要求 Pillow ≥ 12.3，所以：
- 資料夾裡有 `.avif`、沒有裝 pillow-heif 時，會跳「安裝 pillow-heif 以檢視 HEIC/AVIF 影像」。照做也不會有任何改變，AVIF 本來就開得了。
- 匯出、批次匯出、批次轉檔的格式清單沒有 pillow-heif 就不列 AVIF；MCP `convert_format` 轉成 `.avif` 會丟「AVIF output requires the pillow-heif package」。其實 Pillow 自己就寫得出 AVIF。
- `heif_support` 的說明寫「pillow-heif 1.x 一次註冊 HEIF 與 AVIF」，實測 1.4.0 的 `register_heif_opener()` 只註冊 `HEIF`。

**修正**：
- 新增 `image/avif_support.py`：`AVIF_EXTENSIONS` 與 `avif_available()`（`PIL.features.check("avif")`；從原始碼編譯、沒有 libavif 的 Pillow，以及 Windows ARM64 的 wheel 會回 False）。
- `heif_support.HEIF_EXTENSIONS` 拿掉 `.avif`，提示只在有 HEIC / HEIF 時出現，文字（英、簡、繁、日、韓）改成只提 HEIC。
- `formats.STILL_IMAGE_EXTENSIONS` 另外併入 `AVIF_EXTENSIONS`，能開的副檔名不變；`ensure_pillow_opener(".avif")` 不再去註冊 pillow-heif。
- `save_formats.available_formats()`：HEIC 看 pillow-heif、AVIF 看 `avif_available()`、JXL 看 pillow-jxl-plugin，順序固定為 HEIC、AVIF、JXL；`save_image` 對寫不出的 AVIF 丟「AVIF output requires a Pillow built with libavif.」。
- MCP `convert_format` 的工具說明改成 avif 內建、heic / jxl 需要選用後端。
- README ×10 與 docs ×10：CLI 的解碼說明、Save As / Export 的格式、MCP 表格；docs 的「支援格式」補上一行 AVIF / HEIC / JPEG XL，「單張匯出」的格式列表與 MCP `convert_format` 說明（quality 適用 JPEG / WebP / AVIF / HEIC / JXL）一併更新。

**驗證**：
- 新增 `tests/test_avif_support.py`：`avif_available()` 跟著 Pillow 的外掛狀態走；把 `pillow_heif` 設成無法匯入時，`load_image_file` 照樣把 `.avif` 解成正確尺寸與顏色的 RGBA。
- `tests/test_save_formats.py`：沒有 pillow-heif 時 AVIF 仍在清單上、而且真的存得出 RGBA 的 AVIF；Pillow 沒有 libavif 時 AVIF 不在清單、存檔丟出 `ValueError` 且不留檔案；四個選用格式都在時的清單順序。
- `tests/test_heif_support.py`：`.avif` 不算 HEIF，只有 AVIF 的資料夾不需要提示；`tests/test_formats.py`：`.AVIF` 不註冊任何選用 codec。
- `py -m sphinx -W --keep-going -b html docs` 無警告。

## U-20260925-63 · 2026-09-25 · Tool workers report every failure instead of leaving their dialog stuck · #fix #workers

**問題**：約三十個工具對話框各自帶一個 `QThread` worker，按下「套用」後，對話框停用按鈕、等 worker 的 `done` 訊號。但這些 worker 的 `run()` 只接 `(OSError, ValueError)`，有的再加上 `RuntimeError`／`ImportError`。以下例外會直接跑出執行緒：
- `DecompressionBombError`：超過 Pillow 像素上限的大圖，它不是 `OSError`；
- `MemoryError`；
- `cv2.error`；
- 選用後端的 `ImportError`。

這時 `done` 永遠不會發出，對話框的 `_worker` 一直不是 None，之後再按「套用」也沒有反應，只能關掉重開。共用的 `gui/_apply_save.EffectWorker` 早就因為同樣的原因改成全接並記錄，這些各自帶 worker 的對話框沒有跟上。

**修正**：
- 26 個對話框的 worker 原本就在 handler 裡 `logger.exception` 並回報失敗，改成 `except Exception`，和 `EffectWorker` 一致：
  - 單圖工具：CLAHE、HSL 混色、相框、比例尺、網點、色彩對應、扭曲、二值化、Otsu、日曬、輝光、像素排序、局部對比、背景平坦化、梗圖、證件照排版、目標大小、仿製印章、小星球、隱寫；
  - 多圖工具：立體圖、拼貼、影像堆疊、動畫編輯、列印排版；
  - 挑片對話框的自動挑片。
- OCR、套件安裝（`plugin/pip_installer._InstallWorker`）、影片擷取外掛（`video_source`）的預期錯誤原本就回報得比較安靜，不帶 traceback；這三個保留原本的處理，後面再加一層 `except Exception`，先 `logger.exception` 再回報。
- `video_source` 外掛已同步到 Imervue_Plugins 的 `main`。
- `architecture_explore.md` §10 的「Worker 邊界」補上新的檢查測試。

**驗證**：新增 `tests/test_workers_always_report.py`：
- AST 檢查 `Imervue/` 與 `plugins/` 裡每個 `QThread.run` 最上層的 `try`：都要以 `Exception`／`BaseException` 或 bare `except` 收尾，或在 `finally` 發訊號。修正前會列出 29 處。
- CLAHE worker 的 `load_rgba` 丟出 `DecompressionBombError` 時，`done` 送出 `(False, "too many pixels")`。
- OCR worker 遇到 `RuntimeError` 或 `MemoryError` 時照樣送出失敗，並留下 traceback log。

## U-20260925-64 · 2026-09-25 · Every image decode catches a picture over the pixel limit · #fix #robustness

**問題**：有 15 處在一般流程（不是 worker）裡解碼圖片，只接 `(OSError, ValueError)` 或 `OSError`。超過 Pillow 像素上限的圖丟出的 `DecompressionBombError` 不是 `OSError`；EXIF 區塊損壞的 WebP 在 `getexif` 會丟出 `SyntaxError`。這兩種例外會跑出去：
- **修圖配方的影像圖層**（`image/layers._render_image_layer`）：引用的圖一大，整張照片的配方渲染就中斷。
- **相似圖搜尋**（`perceptual_hash.hash_paths`）、**CLIP 語意搜尋的建索引**（`clip_search.embed_image`）：一張超大圖就讓整批結束。
- **UI 裡的單張工具**：自動色彩平衡、頻率分離、人像修飾、智慧裁切、軟打樣、隱寫讀取、小星球的等距柱狀判斷、參考圖面板、右鍵「拆分頁面」的頁數判斷、Paint 拖進檔案、主視窗把目前的圖送進 Paint、puppet 貼圖上傳。例外跑出 Qt slot，操作無聲失敗。
- **ai_smart_resize 外掛**的尺寸探測只接 `OSError`。

**修正**：
- 這 15 處都改接 `image/read_errors.IMAGE_READ_ERRORS`，原本的處理不變（記錄、回報、跳過或回傳 None）。
- ai_smart_resize 外掛不新增對主程式的 import（避免多一條跨 repo 契約），改在原處列出 `(OSError, ValueError, SyntaxError, Image.DecompressionBombError)`；已同步到 Imervue_Plugins 的 `main`。
- `architecture_explore.md` §10 的「讀圖」補上新的檢查測試。

**驗證**：
- 新增 `tests/test_image_reads_catch_every_read_error.py`：AST 檢查 `Imervue/` 與 `plugins/` 裡每個本體呼叫解碼函式的 `try`（`Image.open`、`decode_image(_file)`、`load_rgba`、`open_export_source`、`recipe_base_image`、`upright_image`、`load_image_file`、`_load_shown`），都要接 `IMAGE_READ_ERRORS`、列出 `DecompressionBombError`，或更寬。對修正前的程式跑，會列出 15 處。
- `tests/test_layers.py`：影像圖層引用超過像素上限的圖時回傳 None，並留下 warning。
- `tests/test_perceptual_hash.py`：`hash_paths` 跳過超過像素上限的圖，其他照常雜湊。

## U-20260925-65 · 2026-09-25 · Batch workers report a model or folder that fails before the first image · #fix #workers

**問題**：U-20260925-63 修的是 `run()` 最上層有 `try` 的 worker。還有三個批次 worker 是在迴圈裡逐張接例外，但迴圈前或迴圈外的一步完全沒有保護，失敗時結束訊號不會送出，對話框的「執行」按鈕就一直停用：
- **ai_background_remover 外掛的批次去背**：`new_session(model)` 第一次使用時會下載模型，沒有網路或缺 onnxruntime 時直接丟出例外。
- **safety_review 外掛的「全部掃描」**：`_resolve_detector` 載入 NudeNet／動漫模型失敗時直接丟出例外。
- **影像整理**（`gui/image_organizer_dialog._OrganizerWorker`）：輸出資料夾不能寫入，或子資料夾名稱被同名檔案占用時，`os.makedirs` 直接丟出例外。

**修正**：
- 批次去背：`models` 資料夾建立、`rembg` import、`new_session` 包進 `try`；失敗時記錄 traceback，回報「0 成功、全部失敗」。
- 全部掃描：`_resolve_detector` 包進 `try`；失敗時記錄 traceback，回報 `(0, 總數, 0)`。
- 影像整理：建立某組的子資料夾失敗時，記錄 traceback，把那一組算成失敗，繼續處理下一組。
- 兩個外掛已同步到 Imervue_Plugins 的 `main`。

**驗證**：
- `tests/test_ai_background_remover_cancel.py`：`new_session` 丟出例外時，`result_ready` 送出 `(0, 2)`，並留下 traceback log。同一個檔案裡既有的中斷測試，假的 `PIL` 模組補上 `ImageOps`：原本要靠別的測試先載入真的 `PIL.ImageOps` 才會通過，單獨執行時會失敗。
- 新增 `tests/test_safety_review_workers.py`：`_resolve_detector` 丟出例外時送出 `(0, 3, 0)`，並留下 traceback log。
- `tests/test_image_organizer.py`：一組的子資料夾名稱被檔案占用時，送出 `(1, 1)`，另一組照常複製。

## U-20260925-66 · 2026-09-25 · Canon CR3, Panasonic RW2, Pentax PEF and 14 more RAW formats open · #fix #raw #formats

**問題**：`formats.RAW_EXTENSIONS` 只有 CR2 / NEF / ARW / DNG / RAF / ORF 六種。Canon 從 2018 年起的機身都存 CR3，Panasonic 是 RW2、Pentax 是 PEF、Nikon 小相機是 NRW、Samsung 是 SRW，這些檔案在檢視器、檔案樹、開啟對話框和媒體庫裡根本不會出現，雖然 rawpy 附的 LibRaw 0.22 都讀得了。另外還有兩份各寫各的清單：
- `library/stacks.RAW_EXTENSIONS`（19 種，含 LibRaw 在 rawpy wheel 裡讀不了的 `.x3f`）；
- MCP `tool_support.IMAGE_EXTENSIONS`：列了 CR3 / RW2 / PEF / SRW，但開檔時只有那六種會走 LibRaw，其餘交給 Pillow 就失敗；AVIF、JPEG XL 反而沒列。

**修正**：
- `formats.RAW_EXTENSIONS` 改成 23 種：CR2 / CR3 / CRW、NEF / NRW、ARW / SRF / SR2、DNG、RAF、ORF、RW2 / RWL、PEF、SRW、3FR、IIQ、MEF、MOS、ERF、MRW、KDC / DCR。不收 `.x3f`（LibRaw 要以 `USE_X3FTOOLS` 編譯才讀，rawpy 的 wheel 沒有），也不收太多非相機檔案共用的 `.raw`。
- `library/stacks` 改用 `formats.RAW_EXTENSIONS`，RAW+JPEG 堆疊涵蓋檢視器開得了的每一種 RAW。
- MCP `IMAGE_EXTENSIONS` 改成 `STILL_IMAGE_EXTENSIONS` 去掉 SVG（要 Qt 才能點陣化，伺服器不載 Qt）。
- README ×10 的 rawpy 列出主要 RAW 格式；docs ×10「支援格式」的 RAW 清單改成完整的 23 種。

**驗證**：
- 從 [raw.pixls.us](https://raw.pixls.us/) 下載真實樣本（Canon EOS M50 CR3、Panasonic DC-G9 RW2、Nikon Coolpix P7800 NRW、Samsung NX300 SRW），經 `raw_dimensions`、`develop_raw`（嵌入預覽與完整顯像）、`decode_image_file`、`load_image_file` 與 MCP `read_image_metadata` 都得到正確尺寸（例如 CR3 為 6024×4020），樣本未納入版控。
- `tests/test_formats.py`：CR3、RW2、NRW、PEF、SRW、CRW、3FR、IIQ 與原本六種都在 `RAW_EXTENSIONS` 與 `VIEWER_EXTENSIONS`；`.x3f`、`.raw` 不在。
- `tests/test_image_loader.py`：CR3 / RW2 / NRW / SRW / PEF 都交給 `_load_raw` 顯像。
- `tests/test_stacks.py`：CR3、RW2、PEF 算 RAW，CR3 與同名 JPG 堆成一格，堆疊用的就是 `formats.RAW_EXTENSIONS`。
- `tests/test_imervue_mcp_server.py`：`list_images` 列出 AVIF、JXL、CR3、RW2，不列 SVG；CR3 的 metadata 與轉檔走 LibRaw 顯像後的尺寸。
- 全套 13705 passed；ruff、bandit 無新問題；`sphinx -W` 無警告。

## U-20260925-67 · 2026-09-25 · Sorting by name puts img2 before img10, like the file tree and Explorer · #fix #sort

**問題**：「依名稱排序」用的是純字串比較（`basename.lower()`），所以 `page10.png` 排在 `page1.png` 和 `page2.png` 之間。檔案樹早就用 numeric mode 的 `QCollator` 做自然排序，程式註解也寫著「img2 < img10，符合 Explorer」。結果同一個資料夾在檔案樹是 page1、page2…page10，檢視器的縮圖格和上一張／下一張卻是 page1、page10、page11、page2：看掃描檔、漫畫頁或相機編號時，翻頁順序是亂的。網頁相簿匯出、各批次對話框的檔案清單也是同樣的字串順序。

Windows 檔案總管從 XP 起就預設自然排序（`StrCmpLogicalW`），見 [Natural sort order](https://en.wikipedia.org/wiki/Natural_sort_order)。

**修正**：
- 新增純邏輯模組 `system/natural_sort.py`：`natural_key(name)` 不分大小寫，連續數字依數值比較，全形數字也算；數值相等時（`a01` 與 `a1`、`A` 與 `a`）依小寫、原名定序，順序不受輸入先後影響。
- 以下名稱排序都改用它：
  - 檢視器：`image_loader._scan_images`、`_sort_for_user`；
  - 排序選單：`sort_menu._sort_key_name`；
  - 網頁相簿匯出：`export/gallery_sort`；
  - 剪貼簿貼上後的重排；
  - 批次清單：AI 放大、批次轉檔、重複檔偵測、EXIF 清除、影像整理、影像淨化。
- `image/folder_index` 的資料夾排序快取加上 `format: 2`，舊版以字串順序存下的快取會被忽略並重建。
- README ×10、docs ×10 的「依名稱排序」補上自然排序的說明。

safety_review 外掛的清單排序這次沒改：外掛要 import 新模組，就得在 `architecture.md` §6 多一條跨 repo 契約。

**驗證**：
- 新增 `tests/test_natural_sort.py`：數字依數值、不分大小寫、多段數字、前綴較短者在前、`a01`／`a1` 與大小寫不同時順序固定、全形數字、數字與文字混排不會型別錯誤、長數字串。
- `tests/test_image_loader.py`：資料夾掃描依名稱遞增是 page1、page2、page10，遞減反過來。
- `tests/test_sort_menu.py`、`tests/test_gallery_sort.py`：名稱排序是自然順序。
- `tests/test_folder_index.py`：沒有 `format` 的舊快取回傳 None。

## U-20260925-68 · 2026-09-25 · Animation frames of 10 ms or less play for 100 ms, as in browsers · #fix #animation

**問題**：GIF 的影格延遲以 1/100 秒為單位，很多舊 GIF 寫的是 0 或 1（0／10 ms）。瀏覽器不照字面播：Firefox 一律把 10 ms 以下的影格當成 100 ms，Chrome 跟進，Chromium 原始碼的註解寫著「很多廣告用 0 讓圖片閃得越快越好」（[Mozilla bug 232822](https://bugzilla.mozilla.org/show_bug.cgi?id=232822)、[IEInternals: Animated GIF Timing](https://learn.microsoft.com/vi-vn/archive/blogs/ieinternals/trivia-animated-gif-timing)）。這些 GIF 本來就是照 100 ms 設計的。

`gpu_image_view/actions/animation_player._frame_duration` 只把 0 或缺值當成 100 ms，10 ms 的影格就真的播 10 ms。同一張 GIF 在 Imervue 裡比在瀏覽器快 10 倍。

**修正**：10 ms 以下（含 10 ms）的影格一律播 100 ms，和瀏覽器一致；11 ms 以上照檔案寫的播。README ×10 與 docs ×10 的動畫播放說明補上這一點。

**驗證**：`tests/test_animation_player.py` 新增參數化測試：10、1、10.0 ms 播 100 ms；11、20 ms 照原值。原本 0、None、缺值播 100 ms 的測試照常通過。

## U-20260925-69 · 2026-09-25 · HDR merge, panorama, focus stack, stack blend, paint drops and the CLI develop camera RAW · #fix #raw

**問題**：不靠 Qt 的解碼入口 `shown.load_shown_rgb` / `load_shown_rgba` 與 CLI 的 `_open_shown` 一律交給 Pillow，沒有走 libraw。以 [raw.pixls.us](https://raw.pixls.us/) 的真實樣本實測：
- Nikon D3200 的 NEF 被讀成 IFD0 裡 **160×120** 的縮圖（libraw 顯像是 6034×4012）；Canon EOS 100D 的 CR2 讀成相機內嵌的 JPEG 預覽。HDR 合成、全景、景深合成、堆疊混合拿 NEF 包圍曝光時，實際合成的是一堆 160×120 的小圖。
- CR3、RW2、NRW、SRW 直接 `cannot identify image file`。
- Paint 的素材／姿勢圖拖放同樣經過這個入口。
- CLI：`info` 報的是預覽的尺寸與格式；資料夾掃描不收任何 RAW；`resize` / `strip` 沿用來源副檔名，RAW 會被寫成 `.nef` 名稱。

**修正**：
- `shown.open_shown(path)`：相機 RAW 經 `raw_loader.develop_raw` 顯像（與檢視器相同），其餘照舊註冊 HEIC / JXL codec、轉 sRGB 並依 EXIF 轉正；檔案在回傳前關閉。`load_shown_rgb` / `load_shown_rgba` 改建在它上面，所以 HDR 合成、全景、景深合成、堆疊混合與 Paint 拖放都跟著修好。
- `dimensions.probe_image(path)` 回傳 `(格式, 模式, 寬, 高)`：RAW 是副檔名、`RGB` 與 libraw 的尺寸；`image_dimensions` 改用它，行為不變。
- `formats.RASTER_EXTENSIONS`：靜態格式去掉要 Qt 才能點陣化的 SVG。
- CLI：輸入改走 `open_shown` / `load_shown_rgba`，`info` 改用 `probe_image`，資料夾掃描收 `RASTER_EXTENSIONS`（RAW、APNG、HEIF 都進來），沿用副檔名的輸出遇到 RAW 改寫成 `.png`。
- README ×10 與 docs ×10 的 CLI 說明補上「相機 RAW 像檢視器一樣顯像，`resize` 與 `strip` 寫成 PNG」。這段文字是同時進行的另一筆提交 `532d737` 整份提交 README / docs 時一併帶進去的，本筆補上對應的程式。

**驗證**：
- 真實樣本：CLI `info` 回報 NEF 6034×4012、CR3 6024×4020、RW2 10402×7794；`resize --max 800` 把 NEF / CR3 寫成 800×532 / 800×534 的 PNG。
- `tests/test_shown.py`：Pillow 只看得到 160×120 縮圖的 `.nef` 與 `.CR3` 都交給 `develop_raw`；HDR 合成、全景、景深合成、堆疊混合的載入函式對 CR2 包圍曝光都拿到顯像結果；壞掉的 RAW 丟出 `IMAGE_READ_ERRORS`；`open_shown` 回傳後檔案已關閉（Windows 上可以刪除）且像素已載入。
- `tests/test_cli.py`：`convert` / `resize` / `strip` / `auto-orient` 對 NEF 寫出顯像尺寸的 PNG；`info` 回報 `NEF`、`RGB`、libraw 尺寸；壞掉的 CR3 記為錯誤；資料夾收 CR3、RW2、AVIF、JXL，不收 SVG；`output_path` 對 RAW 改用 `.png`。
